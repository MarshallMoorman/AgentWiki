Document the module "{{ModuleName}}" (id: {{ModuleId}}) for AI coding agents.

Repository: {{RepoName}}
Module summary: {{ModuleSummary}}

Related files:
{{RelatedFiles}}

Respond with a single JSON object only (no markdown fences):
{
  "id": "{{ModuleId}}",
  "title": "string",
  "purpose": "string",
  "entryPoints": ["path or symbol"],
  "dependencies": ["module or package"],
  "keyTypes": ["type or file names"],
  "howToExtend": ["actionable guidance"],
  "gotchas": ["warnings"],
  "relatedFiles": ["paths"]
}

Focus on what an agent needs to change this module safely. Reference real paths only.
Describe current behavior only. Do not call code deprecated/obsolete/legacy unless the source inventory shows explicit deprecation markers.
Keep `purpose` as a plain string. Keep `dependencies`, `entryPoints`, `keyTypes`, `howToExtend`, `gotchas`, and `relatedFiles` as arrays of strings (not nested objects).
